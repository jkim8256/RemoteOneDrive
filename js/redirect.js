const task = document.querySelector(".task");
const input = task.getElementsByID();

task.addEventListener(submit, handleSubmit)

function handleSubmit(event) {
    event.preventDefault();
    if input == 
    location.href = `html/{sdf}.html`;
    
}

document.querySelector(task).onclick = function () {
    task.get
    location.href = "html/packaging.html";
};